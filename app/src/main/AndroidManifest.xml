<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectosemestralv2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <!-->
        <activity android:name=".Especie"></activity>

        <activity android:name=".daoEspecie" />
        -->
        <activity android:name=".addItem" />
        <activity android:name=".registroProveedores" />
        <activity android:name=".registroUsuarios" />
        <activity android:name=".archivoExporExcel" />
        <activity android:name=".menuInicio" />
        <activity android:name=".busquedaProveedor"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>